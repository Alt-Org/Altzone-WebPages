'use client';
import { TeacherSignInCard } from '@/features/TeacherSignIn';

/**
 * TeachersPage is a React functional component that renders the TeacherSignInCard component. It serves as the main page for teachers to sign in.
 * @returns A JSX element containing the TeacherSignInCard component.
 */
const TeachersPage = () => {
    return <TeacherSignInCard />;
};

export default TeachersPage;
