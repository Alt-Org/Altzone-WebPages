import {FC, ReactElement} from "react";
// import {Navigate} from "react-router-dom";
// import {useUserPermissions,UserPermissions} from "@/entities/Auth";


// type GuardedRouteProps = {
//     component: ReactElement;
//     guard: UserPermissions;
//     redirectTo: string;
// };
//
// export const GuardedRoute: FC<GuardedRouteProps> = ({ component, guard, redirectTo }) => {
//     const { canI } = useUserPermissions();
//
//     if (canI(guard)) {
//         return component;
//     } else {
//         return <Navigate to={redirectTo} />;
//     }
// };
