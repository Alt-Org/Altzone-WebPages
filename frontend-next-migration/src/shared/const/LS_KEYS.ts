/**
 * Keys for local storage.
 * @enum {string}
 */
export enum LS_KEYS {
    /**
     * Key for information about the authenticated user.
     */
    AUTH_USER = 'AuthUser',
    /**
     * Key for saving language in local storage.
     */
    LOCAL_STORAGE_LANGUAGE_KEY = 'language',
    /**
     * Key for saving profile data in local storage
     */
    PROFILE = 'profile',
    /**
     * Key for storing the state of navbar being fixed or not.
     */
    IsNavBarFixed = 'NavbarFixed',
}
