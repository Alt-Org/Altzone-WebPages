export interface SocialIconLink {
    name: string;
    icon: string;
    link: string;
}

export interface Texts {
    cookies: string;
    privacy: string;
    consent: string;
    currentYear: number;
}